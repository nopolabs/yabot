parameters:

services:
    plugin.status:
        class: Nopolabs\Yabot\Status\StatusPlugin
        arguments:
            - '@message.dispatcher'
            - '@logger'
            - '@yabot'
        tags:
            - { name: yabot.plugin }
